# CUSTOM: Mutation to record an O with the current video player position.

mutation SceneRecordOAtTimestamp($id: ID!, $video_timestamp: Float!) {
  sceneRecordOAtTimestamp(id: $id, video_timestamp: $video_timestamp) {
    count
    history
  }
}
