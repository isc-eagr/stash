mutation PerformerCreate($input: PerformerCreateInput!) {
  performerCreate(input: $input) {
    ...PerformerListData # CUSTOM: mutation callers and list cache need no role totals
  }
}

mutation PerformerUpdate($input: PerformerUpdateInput!) {
  performerUpdate(input: $input) {
    ...PerformerListData # CUSTOM: preserve list-cache fields without role totals
  }
}

mutation BulkPerformerUpdate($input: BulkPerformerUpdateInput!) {
  bulkPerformerUpdate(input: $input) {
    # CUSTOM: keep bulk edit responses lightweight; callers refetch impacted lists.
    id
  }
}

mutation PerformerDestroy($id: ID!) {
  performerDestroy(input: { id: $id })
}

mutation PerformersDestroy($ids: [ID!]!) {
  performersDestroy(ids: $ids)
}

mutation PerformerMerge($input: PerformerMergeInput!) {
  performerMerge(input: $input) {
    id
  }
}
