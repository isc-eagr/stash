# CUSTOM
query FindTaskProgressMilestones {
  findTaskProgressMilestones {
    ...TaskProgressMilestoneData
  }
}

query TaskProgressMilestone($id: ID!) {
  taskProgressMilestone(id: $id) {
    ...TaskProgressMilestoneData
  }
}
